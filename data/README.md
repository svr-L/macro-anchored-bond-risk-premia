# Data folder

Raw and cached data are intentionally not committed.

- `raw/`: optional local raw files.
- `cache/`: optional downloaded/cached FRED/OECD series.

The notebooks are designed to download public data where possible. Generated data files are ignored by Git by default.
